function weatherForecast(input) {
    let weather = input.toString();
    if (weather == "sunny") {
        console.log("It's warm outside!");
    }
    else {
        console.log("It's cold outside!");
    }
}